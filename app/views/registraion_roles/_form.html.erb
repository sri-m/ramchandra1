<%= form_for(@registraion_role) do |f| %>
  <% if @registraion_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registraion_role.errors.count, "error") %> prohibited this registraion_role from being saved:</h2>

      <ul>
      <% @registraion_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
